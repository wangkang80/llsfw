<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.llsfw.core.mapper.expand.IMapQueryMapper">
    <select id="queryMap" resultType="java.util.HashMap">
        ${sql}
    </select>
    <select id="queryOneRowOneValue" resultType="java.lang.String">
        ${sql}
    </select>
    <delete id="delete">
        ${sql}
    </delete>
    <update id="update">
        ${sql}
    </update>
    <insert id="insert">
        ${sql}
    </insert>
</mapper>
